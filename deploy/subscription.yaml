apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  annotations:
    apps.open-cluster-management.io/git-branch: ##GH_BRANCH##
    apps.open-cluster-management.io/git-path: ##GH_PATH##
  name: demo-policies
spec:
  channel: policies/deploy-stable-policies
  placement:
    local: true
